query GetDoctors($page: PaginationInput!) {
  doctors(page: $page) {
    doctors {
      license
      firstName
      lastName
      email
      contacts
      specialties
    }    
    paginationToken
  }
}
